// js para abrir el modal y cerrarlo
/**const openModalBtn = document.getElementById('openModalBtn');
const modal = document.getElementById('modal');
const closeModalBtn = document.querySelector('.close-btn');

// abirir modal
openModalBtn.onclick = function() {
    modal.style.display = 'block';
}

// Cerrar modal
closeModalBtn.onclick = function() {
    modal.style.display = 'none';
}

// Cerrar modal al hacer clic fuera de ella
window.onclick = function(event) {
    if (event.target == modal) {
        modal.style.display = 'none';
    }
}

// Manejar el clic del botón de avatar (cambiar color y texto)
const avatarButtons = document.querySelectorAll('.avatar-btn');

avatarButtons.forEach(button => {
    button.addEventListener('click', function() {
        button.innerText = 'Obtenido';
        button.classList.add('obtenido');
    });
});
*/

/**json de perfil de usuario */

